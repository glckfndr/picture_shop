<div class="w-full mx-auto md:w-2/3">
  <h1 class="text-3xl font-bold">New Order</h1>
  <div>
    <!-- products table -->
    <%= render 'orders/products', products: @order_products[:products], total_sum: @order_products[:total_sum] %>
  </div>
  <%= render "orders/form", order: @order %>
  <%= link_to 'Return to orders', orders_path, class:"green-xs-btn justify-left" %>
</div>
